package co.tripzii.station.model

class ImageModel {

    private var imageDrawable: Int = 0

    fun getImageDrawables(): Int {
        return imageDrawable
    }

    fun setImageDrawables(imageDrawable: Int) {
        this.imageDrawable = imageDrawable
    }
}